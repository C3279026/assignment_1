#include <stdio.h>
void zeros(int*alphnos);
int main(){
    int a;
    int alphnos[26];
    char message[]"HELLO";
    message[i]=message[i]-65;
    printf("%d", message[i]);
}

void zero( int*alphnos) {
int i; // Array index loop counter
for(i = 0; i < N; i++)
alphnos[i] = 0; // Use array syntax
return; /